import { ShieldCheck } from 'lucide-react';

export function Logo() {
  return (
    <div className="bg-primary text-primary-foreground rounded-full p-3 w-fit">
      <ShieldCheck className="h-10 w-10" />
    </div>
  );
}
